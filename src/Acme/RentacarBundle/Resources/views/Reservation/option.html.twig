{% extends "AcmeRentacarBundle::base.html.twig" %}

{% block page_title 'RESERVATION/OPTION' %}
{% set menu = 'reservation' %}

{% block content %}
{# {% include "AcmeRentacarBundle::_car.html.twig" with {'carClass':reservation.carClass} %} #}

<form action="{{ path('reservation_option') }}" method="post">
    {# {{ form_error(form) }} #}
    <fieldset>
        <legend>OPTION</legend>
        <div class="control-group">
            <label class="option-label">免責補償</label>
            <div class="controls">
                <label class="checkbox">{# {{ form_widget(form.userInsurance) }} #} 免責補償をつける</label>
            </div>
        </div>
        <div class="control-group">
            <label class="control-label">備考（任意）</label>
            <div class="controls">
                {# {{ form_widget(form.note, {'attr':{'class':'input-xlarge', 'row':5} }) }} #}
            </div>
        </div>
        {# {{ form_reset(form) }} #}
        <div class="form-actions">
            <input type="submit" class="btn btn-primary" value="予約内容を確認する">
        </div>
    </fieldset>
</form>

{% endblock %}
